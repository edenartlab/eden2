name: Talking Head
description: Talking head generated with Hedra API
tip: |-
  This tool can be used for creating a talking head
cost_estimate: 200
output_type: video
status: stage
visible: false
parameters:
  image:
    type: image
    label: Input image
    description: The image to turn into a talking head
    required: true
  audio:
    type: audio
    label: Input audio
    description: The audio to turn into a talking head
    required: true
  aspectRatio:
    type: str
    label: Aspect ratio
    description: The aspect ratio of the video
    default: '1:1'
    choices: ['16:9', '1:1', '9:16']